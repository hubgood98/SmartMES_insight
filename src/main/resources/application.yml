spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartmes?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  jwt:
    secret: YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXo=
    expiration: 3600000   # 1시간 (ms)